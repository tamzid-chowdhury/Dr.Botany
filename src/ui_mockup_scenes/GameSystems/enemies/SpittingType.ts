import Scene from "../../../Wolfie2D/Scene/Scene";
import EnemyType from "./EnemyType";

export default class Spitting extends EnemyType {

    initialize(options: Record<string, any>): void {

	};

    doAnimation(...args: any): void {

	};

    createRequiredAssets(scene: Scene): Array<any> {
		return []
	};
}